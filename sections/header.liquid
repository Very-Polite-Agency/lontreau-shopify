{% assign section_name = 'header' %}
{% assign section_id = section_name | append: '--' | append: section.id %}

{% assign menu_main = section.settings.menu_main | default: false %}

<div class="{{ section_name }}__main">

  <div class="{{ section_name }}__brand">
    <a href="/" target="_self" title="Home">
      {% render 'svg', type: 'brand.logo.circle' %}
    </a>
  </div>

  {% if menu_main %}
    <nav class="{{ section_name }}__main-navigation navigation navigation--main">
      {% for link in linklists[menu_main].links %}
        {% render 'navigation-item', link: link %}
      {% endfor %}
    </nav>
  {% endif %}

  <nav class="{{ section_name }}__cart-navigation navigation navigation--cart">
    <div class="navigation__item">
      {% assign link_active = false %}
      {% if template == 'cart' %}
        {% assign link_active = true %}
      {% endif %}
      <a class="navigation__link" href="/cart" title="Cart">
        <span>{% render 'svg', type: 'icon.cart' %}</span>
        <span>({{ cart.item_count }})</span>
      </a>
    </div>
  </nav>

  <button class="{{ section_name }}__button button d-lg-none" type="button">
    <span class="button__title">Menu</span>
  </button>

</div>

{% stylesheet %}
{% endstylesheet %}

{% javascript %}
{% endjavascript %}

{% schema %}
  {
    "name": "Header",
    "tag": "header",
    "class": "header",
    "settings": [
      {
        "content": "Navigation",
        "type": "header"
      },
      {
        "label": "Main Menu",
        "type": "link_list",
        "id": "menu_main"
      }
    ],
	  "blocks": []
  }
{% endschema %}
