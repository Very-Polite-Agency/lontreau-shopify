{%- if type and type != blank -%}
  <div class="badge" data-type="{{ type }}">
    {% case type %}
      {% when 'sale' %}
        <div class="badge__title">Promo</div>
    {% endcase %}
  </div>
{% endif %}
