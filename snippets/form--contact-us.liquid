{%- form 'contact', id: 'form--contact-us', class: 'form form--contact-us js--validate-me' -%}

      <div class="form__main">

            <div class="form__field field">
                  {% render 'label', classes: 'label--form-field', value: 'Rude' %}
                  {% render 'input', classes: 'input--primary input--rude', name: 'contact[rude]' %}
            </div>

            <div class="form__field field">
                  {% render 'label', classes: 'label--form-field', value: 'First Name' %}
                  {% render 'input', classes: 'input--primary input--text', name: 'contact[first_name]' %}
            </div>

            <div class="form__field field">
                  {% render 'label', classes: 'label--form-field', value: 'Last Name' %}
                  {% render 'input', classes: 'input--primary input--text', name: 'contact[last_name]' %}
            </div>

            <div class="form__field field error">
                  {% render 'label', classes: 'label--form-field', value: 'Email' %}
                  {% render 'input', classes: 'input--primary input--email', name: 'contact[email]' %}
            </div>

            <div class="form__field field">
                  {% render 'label', classes: 'label--form-field', value: 'Company' %}
                  {% render 'input', classes: 'input--primary input--company', name: 'contact[company]' %}
            </div>

            <div class="form__field field">
                  {% render 'label', classes: 'label--form-field', value: 'How Can We Help You?' %}
                  {% render 'textarea', classes: 'textarea--primary textarea--body', name: 'contact[body]' %}
            </div>

            <div class="form__field">
                  <button class="button--submit" type="submit">Contact Us</button>
            </div>

      </div>


      <div class="form__loading">
            <div class="form__spinner"></div>
      </div>

{%- endform -%}
